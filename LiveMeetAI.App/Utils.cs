namespace LiveMeetAI.Utils
{
    // Put small helper types here later (file conversions, settings helpers, etc.)
    // For now this placeholder prevents "namespace not found" compile errors.
    public static class Placeholder
    {
        // Example helper signature (no-op). Replace as needed.
        public static string? GetSetting(string key) => null;
    }
}
